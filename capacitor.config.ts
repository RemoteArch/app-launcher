import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.remotearch.applauncher',
  appName: 'app-launcher',
  webDir: 'dist'
};

export default config;
